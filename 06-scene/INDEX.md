# 06 — Scene

**Open this when** the job is arranging many things in one world rather than making one thing: large-scene layout, linking and overrides, or dressing a space so it tells a story.

Paths below are relative to this file's own directory.

| Manual | Use when | Path |
|--------|----------|------|
| `scene-assembly` | Large scene layout — collections, linking, library overrides, streaming-friendly organization, assembling sets from many assets | `scene-assembly/MANUAL.md` |
| `set-dressing` | Placement and environmental storytelling — prop density, narrative clusters, readability rules for interiors and exteriors | `set-dressing/MANUAL.md` |

## Related

- The order to assemble a scene in (blockout → camera → light → …) → `${COMMANDCODE_SKILL_DIR}/references/assembly-order.md`
- Populating the scene with procedural vegetation or props → `geometry-nodes` / `vegetation-artist` in `../02-modeling/INDEX.md`
